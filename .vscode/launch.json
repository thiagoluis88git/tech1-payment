{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/api",
            "args": [
                "-PATH_REDOC_FOLDER",
                "./docs/swagger.json",
                "-localDev",
                "true"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}